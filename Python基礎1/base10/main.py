#オブジェクト指向の概念
#カプセル化→データとそのデータを使った処理を人まとまりにしてクラスに持たせる
#カプセル化のメリット→内部の処理や状態を意識せず、オブジェクトに対する操作
#変更をする時にクラスの外に影響が及びにくい


#継承→親クラスの性質を子クラスが引き継ぐ
#設計（親クラス）で作成したオブジェクトの性質を持つことができるが
#引き継がせたくないものが、あるので最近は継承を使用される機会は減ってきている

#ポリモフィズム→親クラスに異なるオブジェクトを追加したり、異なるクラスを
# 持たせることができるので、オブジェクト指向では一番使われる傾向にある。    
